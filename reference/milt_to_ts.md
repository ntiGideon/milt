# Convert a MiltSeries to a base R ts object

Convert a MiltSeries to a base R ts object

## Usage

``` r
milt_to_ts(series)
```

## Arguments

- series:

  A `MiltSeries` object. Must be univariate.

## Value

A `ts` object.
