% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-ensemble.R
\name{milt_ensemble}
\alias{milt_ensemble}
\title{Create an ensemble milt model}
\usage{
milt_ensemble(models, method = c("mean", "median", "weighted"), weights = NULL)
}
\arguments{
\item{models}{A \strong{named} list of unfitted \code{MiltModel} objects created with
\code{\link[=milt_model]{milt_model()}}.  Each model is independently fitted on the same training
series inside \code{\link[=milt_fit]{milt_fit()}}.}

\item{method}{Aggregation method for combining member point forecasts:
\itemize{
\item \code{"mean"} (default) — simple average.
\item \code{"median"} — element-wise median (more robust to outliers).
\item \code{"weighted"} — weighted average; supply weights via \code{weights}.
}}

\item{weights}{Numeric vector of the same length as \code{models}, used when
\code{method = "weighted"}.  Values need not sum to 1 (they are normalised
internally). \code{NULL} uses equal weights.}
}
\value{
An unfitted \code{MiltModel} of class \code{"MiltEnsemble"}.
}
\description{
Combines multiple models into a single \code{MiltModel} that aggregates their
forecasts.  The ensemble is fitted with \code{\link[=milt_fit]{milt_fit()}} and forecasted with
\code{\link[=milt_forecast]{milt_forecast()}} just like any other model.
}
\examples{
\donttest{
s <- milt_series(AirPassengers)
ens <- milt_ensemble(
  models = list(naive = milt_model("naive"), drift = milt_model("drift")),
  method = "mean"
) |>
  milt_fit(s) |>
  milt_forecast(12)
print(ens)
}
}
\seealso{
\code{\link[=milt_model]{milt_model()}}, \code{\link[=milt_fit]{milt_fit()}}, \code{\link[=milt_forecast]{milt_forecast()}}, \code{\link[=milt_compare]{milt_compare()}}

Other model: 
\code{\link{milt_backtest}()},
\code{\link{milt_compare}()},
\code{\link{milt_cv}()},
\code{\link{milt_fit}()},
\code{\link{milt_forecast}()},
\code{\link{milt_local_model}()},
\code{\link{milt_model}()},
\code{\link{milt_predict}()},
\code{\link{milt_refit}()},
\code{\link{milt_residuals}()}
}
\concept{model}
