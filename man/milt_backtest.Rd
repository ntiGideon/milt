% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{milt_backtest}
\alias{milt_backtest}
\title{Walk-forward backtesting of a milt model}
\usage{
milt_backtest(
  model,
  series,
  horizon,
  initial_window = NULL,
  stride = 1L,
  method = c("expanding", "sliding"),
  window = NULL,
  metrics = c("MAE", "RMSE", "MAPE")
)
}
\arguments{
\item{model}{An unfitted \code{MiltModel} (created with \code{\link[=milt_model]{milt_model()}}).
The model is cloned and re-fitted from scratch on each fold's training
window; the original object is not modified.}

\item{series}{A \code{MiltSeries} object containing the full time series.}

\item{horizon}{Positive integer. Forecast horizon for each fold.}

\item{initial_window}{Positive integer. Size of the first training window.
Defaults to \code{max(floor(n * 0.5), horizon + 1L)}.}

\item{stride}{Positive integer. Number of steps to advance the training
cutoff between consecutive folds. Default \code{1L}.}

\item{method}{Character scalar: \code{"expanding"} (training window grows each
fold) or \code{"sliding"} (training window is of fixed size, specified by
\code{window}). Default \code{"expanding"}.}

\item{window}{Positive integer. Only used when \code{method = "sliding"}.
Size of the sliding training window. Defaults to \code{initial_window}.}

\item{metrics}{Character vector of metric names to compute. Supported:
\code{"MAE"}, \code{"RMSE"}, \code{"MSE"}, \code{"MAPE"}, \code{"SMAPE"}. Default
\code{c("MAE", "RMSE", "MAPE")}.}
}
\value{
A \code{MiltBacktest} object.
}
\description{
Performs time series cross-validation using either an \strong{expanding} or a
\strong{sliding} window strategy, computing forecast accuracy metrics for each
fold.
}
\examples{
\donttest{
s  <- milt_series(AirPassengers)
bt <- milt_backtest(milt_model("naive"), s, horizon = 12)
print(bt)
as_tibble(bt)
}
}
\seealso{
\code{\link[=milt_model]{milt_model()}}, \code{\link[=milt_fit]{milt_fit()}}, \code{\link[=milt_forecast]{milt_forecast()}}, \code{\link[=milt_accuracy]{milt_accuracy()}}

Other model: 
\code{\link{milt_compare}()},
\code{\link{milt_cv}()},
\code{\link{milt_ensemble}()},
\code{\link{milt_fit}()},
\code{\link{milt_forecast}()},
\code{\link{milt_local_model}()},
\code{\link{milt_model}()},
\code{\link{milt_predict}()},
\code{\link{milt_refit}()},
\code{\link{milt_residuals}()}
}
\concept{model}
