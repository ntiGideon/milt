% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{milt_compare}
\alias{milt_compare}
\title{Compare multiple milt models via walk-forward backtesting}
\usage{
milt_compare(
  models,
  series,
  horizon,
  initial_window = NULL,
  stride = 1L,
  method = c("expanding", "sliding"),
  metrics = c("MAE", "RMSE", "MAPE"),
  rank_metric = metrics[[1L]]
)
}
\arguments{
\item{models}{A \strong{named} list of unfitted \code{MiltModel} objects created with
\code{\link[=milt_model]{milt_model()}}. Names become the model labels in the comparison table.}

\item{series}{A \code{MiltSeries} object.}

\item{horizon}{Positive integer. Forecast horizon for each fold.}

\item{initial_window}{Positive integer or \code{NULL}. Passed to
\code{\link[=milt_backtest]{milt_backtest()}}.}

\item{stride}{Positive integer. Steps between consecutive folds. Default
\code{1L}.}

\item{method}{\code{"expanding"} or \code{"sliding"}. Default \code{"expanding"}.}

\item{metrics}{Character vector of metric names. Default
\code{c("MAE", "RMSE", "MAPE")}.}

\item{rank_metric}{Character scalar. Metric used to rank models in the
summary. Must be one of \code{metrics}. Default \code{metrics[[1L]]}.}
}
\value{
A \code{MiltComparison} object.
}
\description{
Runs \code{\link[=milt_backtest]{milt_backtest()}} on each model and collects results into a ranked
comparison table.
}
\examples{
\donttest{
s <- milt_series(AirPassengers)
cmp <- milt_compare(
  models = list(naive = milt_model("naive"), drift = milt_model("drift")),
  series = s, horizon = 12,
  initial_window = 120L, stride = 12L
)
print(cmp)
}
}
\seealso{
\code{\link[=milt_backtest]{milt_backtest()}}, \code{\link[=milt_model]{milt_model()}}

Other model: 
\code{\link{milt_backtest}()},
\code{\link{milt_cv}()},
\code{\link{milt_ensemble}()},
\code{\link{milt_fit}()},
\code{\link{milt_forecast}()},
\code{\link{milt_local_model}()},
\code{\link{milt_model}()},
\code{\link{milt_predict}()},
\code{\link{milt_refit}()},
\code{\link{milt_residuals}()}
}
\concept{model}
