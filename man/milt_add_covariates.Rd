% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series-covariates.R
\name{milt_add_covariates}
\alias{milt_add_covariates}
\title{Add covariates to a MiltSeries}
\usage{
milt_add_covariates(
  series,
  covariates,
  type,
  time_col = NULL,
  group_col = NULL
)
}
\arguments{
\item{series}{A \code{MiltSeries} object.}

\item{covariates}{A data frame or tibble containing the covariate columns.
For \code{"past"} and \code{"future"} types it must include a time column matching
\code{time_col}. For \code{"static"} it must include a column matching \code{group_col}.}

\item{type}{One of \code{"past"}, \code{"future"}, or \code{"static"}.}

\item{time_col}{Name of the time column in \code{covariates}. Defaults to the
same time column name as the series.}

\item{group_col}{Name of the group column in \code{covariates} (static only).
Defaults to the group column of the series.}
}
\value{
The same \code{MiltSeries} with covariates stored internally. The
underlying data tibble is \strong{not} modified; covariates are kept separate
and accessed by models via the private fields.
}
\description{
Attaches external covariate data to a \code{MiltSeries}. Three types are
supported:
}
\details{
\itemize{
\item \strong{past}: time-varying covariates observed in the past (same time range as
the series). Used for in-sample feature enrichment.
\item \strong{future}: time-varying covariates available beyond the series end (e.g.,
calendar, weather forecasts). Used by models to produce covariate-informed
forecasts.
\item \strong{static}: scalar attributes per group that do not vary over time (e.g.,
store size, region). Only meaningful for multi-series.
}
}
\examples{
s <- milt_series(AirPassengers)
dates <- s$as_tibble()$time
cov_df <- data.frame(time = dates, month_num = as.integer(format(dates, "\%m")))
s2 <- milt_add_covariates(s, cov_df, type = "past", time_col = "time")
}
\seealso{
\code{\link[=milt_series]{milt_series()}}

Other series: 
\code{\link{milt_concat}()},
\code{\link{milt_diagnose}()},
\code{\link{milt_fill_gaps}()},
\code{\link{milt_get_covariates}()},
\code{\link{milt_head}()},
\code{\link{milt_plot_acf}()},
\code{\link{milt_plot_decomp}()},
\code{\link{milt_resample}()},
\code{\link{milt_series}()},
\code{\link{milt_split}()},
\code{\link{milt_split_at}()},
\code{\link{milt_tail}()},
\code{\link{milt_window}()},
\code{\link{plot.MiltSeries}()}
}
\concept{series}
