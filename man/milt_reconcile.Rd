% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical.R
\name{milt_reconcile}
\alias{milt_reconcile}
\title{Reconcile hierarchical time series forecasts}
\usage{
milt_reconcile(forecasts, S, method = "ols", residuals = NULL)
}
\arguments{
\item{forecasts}{A named list of \code{MiltForecast} objects, one per node
(both aggregate and bottom-level).  All must share the same horizon.}

\item{S}{Integer/numeric matrix.  Summing matrix with \code{nrow(S)} equal to the
total number of series (length of \code{forecasts}) and \code{ncol(S)} equal to the
number of bottom-level series.}

\item{method}{Character.  Reconciliation method: \code{"ols"} (default),
\code{"wls_struct"}, or \code{"mint_shrink"}.}

\item{residuals}{Optional named list of numeric vectors (in-sample residuals
per series).  Required for \code{"mint_shrink"}.}
}
\value{
A \code{MiltReconciliation} object.
}
\description{
Adjusts a set of base forecasts so that they are coherent with a
user-supplied summing matrix \code{S}.  Three methods are available:
}
\details{
\itemize{
\item \code{"ols"} — ordinary-least-squares reconciliation (equal weights).
\item \code{"wls_struct"} — WLS with structural scaling (diagonal of \code{S \%*\% t(S)}).
\item \code{"mint_shrink"} — MinT with shrinkage covariance estimate (requires the
in-sample residuals supplied via \code{residuals}).
}
}
\examples{
\donttest{
# Two-level hierarchy: Total = A + B
S <- matrix(c(1, 1, 1, 0, 0, 1), nrow = 3, ncol = 2,
            dimnames = list(c("Total", "A", "B"), c("A", "B")))
# (forecast each series first, then reconcile)
}
}
\seealso{
\code{\link[=milt_forecast]{milt_forecast()}}
}
\concept{hierarchical}
