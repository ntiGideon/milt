% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{milt_serve}
\alias{milt_serve}
\title{Deploy a milt model as a REST API}
\usage{
milt_serve(model, host = "127.0.0.1", port = 8000L, launch = TRUE)
}
\arguments{
\item{model}{A fitted \link{MiltModel}.}

\item{host}{Character. Bind address. Default \code{"127.0.0.1"}.}

\item{port}{Integer. TCP port. Default \code{8000L}.}

\item{launch}{Logical. If \code{TRUE} (default) the server is started
interactively (blocking).  Set to \code{FALSE} to return the plumber router
object without starting it.}
}
\value{
The \code{plumber} router object (invisibly when \code{launch = TRUE}).
}
\description{
Generates a Plumber API with standardised endpoints and launches it on
the given host/port.  Requires the \code{plumber} package.
}
\details{
\strong{Endpoints generated:}
\itemize{
\item \code{GET  /health} — returns package version and model name.
\item \code{POST /forecast} — accepts JSON \verb{\{"horizon": <int>\}}, returns forecast.
\item \code{GET  /series_info} — returns metadata about the training series.
}
}
\examples{
\donttest{
m <- milt_model("naive") |> milt_fit(milt_series(AirPassengers))
# milt_serve(m, launch = FALSE)  # returns router without starting
}
}
\seealso{
\code{\link[=milt_save]{milt_save()}}, \code{\link[=milt_dashboard]{milt_dashboard()}}

Other deploy: 
\code{\link{milt_dashboard}()}
}
\concept{deploy}
