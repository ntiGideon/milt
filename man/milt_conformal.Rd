% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{milt_conformal}
\alias{milt_conformal}
\title{Wrap a milt model with conformal prediction intervals}
\usage{
milt_conformal(
  model,
  series,
  horizon,
  level = c(80, 95),
  initial_window = NULL,
  stride = 1L,
  method = c("expanding", "sliding"),
  window = NULL
)
}
\arguments{
\item{model}{An unfitted \code{MiltModel} (created with \code{\link[=milt_model]{milt_model()}}). It is
cloned and refit from scratch on each calibration fold, then refit once
more on the full \code{series} for the final forecast; the original object
passed in is not modified.}

\item{series}{A \code{MiltSeries} used both for calibration folds and as the
training data for the final forecast.}

\item{horizon}{Positive integer. Forecast horizon.}

\item{level}{Numeric vector of confidence levels, e.g. \code{c(80, 95)}.}

\item{initial_window}{Positive integer. Size of the first calibration
training window. Defaults to \code{max(floor(n * 0.5), horizon + 1L)}.}

\item{stride}{Positive integer. Steps between calibration folds. Default
\code{1L}.}

\item{method}{Character scalar: \code{"expanding"} or \code{"sliding"} calibration
window, same semantics as \code{\link[=milt_backtest]{milt_backtest()}}.}

\item{window}{Positive integer. Only used when \code{method = "sliding"}.}
}
\value{
A \code{MiltForecast} object whose prediction intervals are
conformal-calibrated rather than model-native. The point forecast is
unchanged from the underlying model.
}
\description{
Calibrates model-agnostic prediction intervals using split-conformal
prediction. The model is refit on a series of walk-forward calibration
folds (same fold structure as \code{\link[=milt_backtest]{milt_backtest()}}); the resulting
per-horizon-step absolute forecast errors give empirical quantiles that
are applied as a \verb{point ± margin} interval around the final forecast —
regardless of whether the underlying model natively produces prediction
intervals.
}
\examples{
\donttest{
s   <- milt_series(AirPassengers)
fct <- milt_conformal(milt_model("naive"), s, horizon = 12)
plot(fct)
}
}
\seealso{
\code{\link[=milt_backtest]{milt_backtest()}}, \code{\link[=milt_forecast]{milt_forecast()}}

Other model:
\code{\link[=milt_backtest]{milt_backtest()}},
\code{\link[=milt_compare]{milt_compare()}},
\code{\link[=milt_cv]{milt_cv()}},
\code{\link[=milt_ensemble]{milt_ensemble()}},
\code{\link[=milt_fit]{milt_fit()}},
\code{\link[=milt_forecast]{milt_forecast()}},
\code{\link[=milt_global_model]{milt_global_model()}},
\code{\link[=milt_grid_search]{milt_grid_search()}},
\code{\link[=milt_local_model]{milt_local_model()}},
\code{\link[=milt_model]{milt_model()}},
\code{\link[=milt_predict]{milt_predict()}},
\code{\link[=milt_refit]{milt_refit()}},
\code{\link[=milt_residuals]{milt_residuals()}}
}
\concept{model}
