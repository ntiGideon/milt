% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/changepoint.R
\name{milt_changepoints}
\alias{milt_changepoints}
\title{Detect changepoints in a time series}
\usage{
milt_changepoints(
  series,
  method = "pelt",
  stat = "mean",
  penalty = "BIC",
  n_cpts = NA,
  ...
)
}
\arguments{
\item{series}{A \link{MiltSeries} object (univariate).}

\item{method}{Character. Search method: \code{"pelt"} (default), \code{"binseg"}, or
\code{"amoc"} (at-most-one-changepoint).}

\item{stat}{Character. Test statistic: \code{"mean"} (default), \code{"variance"},
or \code{"meanvar"}.}

\item{penalty}{Character. Penalty type passed to the \code{changepoint} package.
Default \code{"BIC"}.}

\item{n_cpts}{Integer or \code{NA}. Maximum number of changepoints for
\code{"binseg"}. Ignored for other methods. Default \code{NA}.}

\item{...}{Additional arguments forwarded to the \code{changepoint} function.}
}
\value{
A \code{MiltChangepoints} object.
}
\description{
Wraps the \code{changepoint} package to identify structural breaks in the mean,
variance, or both.
}
\examples{
\donttest{
s  <- milt_series(AirPassengers)
cp <- milt_changepoints(s, method = "pelt", stat = "mean")
plot(cp)
}
}
\seealso{
\code{\link[=milt_detector]{milt_detector()}}, \code{\link[=milt_detect]{milt_detect()}}

Other anomaly: 
\code{\link{milt_causal_impact}()},
\code{\link{milt_detect}()},
\code{\link{milt_detector}()}
}
\concept{anomaly}
