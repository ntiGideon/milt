% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series-create.R
\name{milt_series}
\alias{milt_series}
\title{Create a MiltSeries object}
\usage{
milt_series(
  x,
  time_col = NULL,
  value_cols = NULL,
  group_col = NULL,
  frequency = NULL,
  start = c(1L, 1L),
  value_col = NULL,
  ...
)
}
\arguments{
\item{x}{Input data. One of:
\itemize{
\item A \code{ts} or \code{mts} object
\item An \code{xts} object
\item A \code{zoo} object
\item A \code{tsibble}
\item A \code{data.frame} or \code{tibble}
\item A numeric vector (requires \code{frequency} and \code{start})
}}

\item{time_col}{Name of the time column when \code{x} is a data frame or tibble.
Auto-detected when \code{NULL}.}

\item{value_cols}{Character vector of value column names. Auto-detected when
\code{NULL} (all non-time, non-group columns).}

\item{group_col}{Name of the grouping column for multi-series data frames.
\code{NULL} for single series.}

\item{frequency}{Frequency label (\code{"monthly"}, \code{"quarterly"}, \code{"daily"},
etc.) or a numeric value. Auto-detected from the time index when \code{NULL}.}

\item{start}{For numeric vector input only: a length-2 integer vector
\code{c(year, period)}, matching the convention of \code{\link[stats:ts]{stats::ts()}}.}

\item{value_col}{Convenience alias for \code{value_cols} when creating a
single-component series.}

\item{...}{Additional arguments passed to underlying conversion methods.}
}
\value{
A \code{MiltSeries} object.
}
\description{
The main entry point for constructing time series objects in milt. Accepts
a wide variety of input formats and returns a consistent \code{MiltSeries}.
}
\examples{
# From a base R ts object
s <- milt_series(AirPassengers)
print(s)

# From a data.frame
df <- data.frame(
  date  = seq(as.Date("2020-01-01"), by = "month", length.out = 24),
  sales = cumsum(rnorm(24, 100, 10))
)
s2 <- milt_series(df, time_col = "date", value_cols = "sales")

# From a numeric vector
s3 <- milt_series(as.numeric(AirPassengers), frequency = 12,
                  start = c(1949, 1))

}
\seealso{
\code{\link[=milt_split]{milt_split()}}, \code{\link[=milt_window]{milt_window()}}, \code{\link[=milt_fill_gaps]{milt_fill_gaps()}},
\code{\link[=milt_diagnose]{milt_diagnose()}}

Other series: 
\code{\link{milt_add_covariates}()},
\code{\link{milt_concat}()},
\code{\link{milt_diagnose}()},
\code{\link{milt_fill_gaps}()},
\code{\link{milt_get_covariates}()},
\code{\link{milt_head}()},
\code{\link{milt_plot_acf}()},
\code{\link{milt_plot_decomp}()},
\code{\link{milt_resample}()},
\code{\link{milt_split}()},
\code{\link{milt_split_at}()},
\code{\link{milt_tail}()},
\code{\link{milt_window}()},
\code{\link{plot.MiltSeries}()}
}
\concept{series}
