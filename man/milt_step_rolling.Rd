% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features-rolling.R
\name{milt_step_rolling}
\alias{milt_step_rolling}
\title{Add rolling-window summary features to a MiltSeries}
\usage{
milt_step_rolling(series, windows = c(7L, 14L, 30L), fns = c("mean", "sd"))
}
\arguments{
\item{series}{A univariate \code{MiltSeries}.}

\item{windows}{Integer vector of window sizes (in number of observations).
Default \code{c(7L, 14L, 30L)}.}

\item{fns}{Character vector of summary functions to apply within each window.
Supported: \code{"mean"}, \code{"sd"}, \code{"median"}, \code{"min"}, \code{"max"}, \code{"sum"}.
Default \code{c("mean", "sd")}.}
}
\value{
An augmented \code{MiltSeries} with additional rolling-feature columns
and \code{max(windows) - 1} fewer rows.  Attribute \code{"milt_step_rolling"}
stores the step specification.
}
\description{
For each combination of \code{windows} and \code{fns}, appends one column named
\verb{.rolling_<fn>_<window>} to the series tibble.  The first \code{max(windows) - 1}
rows (where the window is incomplete) are dropped.
}
\examples{
s     <- milt_series(AirPassengers)
s_rol <- milt_step_rolling(s, windows = c(3L, 6L), fns = "mean")
head(s_rol$as_tibble())
}
\seealso{
\code{\link[=milt_step_lag]{milt_step_lag()}}, \code{\link[=milt_step_fourier]{milt_step_fourier()}}, \code{\link[=milt_step_calendar]{milt_step_calendar()}}

Other features: 
\code{\link{milt_step_calendar}()},
\code{\link{milt_step_fourier}()},
\code{\link{milt_step_lag}()}
}
\concept{features}
