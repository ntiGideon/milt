% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossval.R
\name{milt_cv}
\alias{milt_cv}
\title{Time series cross-validation}
\usage{
milt_cv(
  model,
  series,
  folds = 5L,
  horizon = 1L,
  initial_window = NULL,
  metrics = c("MAE", "RMSE", "MAPE")
)
}
\arguments{
\item{model}{An unfitted \code{MiltModel} created with \code{\link[=milt_model]{milt_model()}}.}

\item{series}{A \code{MiltSeries} object.}

\item{folds}{Positive integer. Number of evaluation folds. Default \code{5L}.}

\item{horizon}{Positive integer. Forecast horizon per fold. Default \code{1L}.}

\item{initial_window}{Positive integer or \code{NULL}. Size of the first
training window.  Defaults to \code{floor(n * (folds / (folds + 1)))}.}

\item{metrics}{Character vector of metric names. Supported: \code{"MAE"},
\code{"RMSE"}, \code{"MSE"}, \code{"MAPE"}, \code{"SMAPE"}. Default \code{c("MAE", "RMSE", "MAPE")}.}
}
\value{
A \code{MiltBacktest} object (same as \code{\link[=milt_backtest]{milt_backtest()}}).
}
\description{
A convenience wrapper around \code{\link[=milt_backtest]{milt_backtest()}} that automatically computes
a stride so that exactly \code{folds} evaluation folds are produced.  The
training window expands fold by fold (expanding window) — no future data is
ever used in training.
}
\details{
The stride is computed so that fold cut-points are approximately evenly
spaced across the remaining data: \cr
\code{stride = max(1, floor((n - horizon - initial_window) / (folds - 1)))}

When \code{folds = 1}, the stride is irrelevant and a single evaluation window is
used (from \code{initial_window} to \code{n - horizon}).
}
\examples{
\donttest{
s  <- milt_series(AirPassengers)
cv <- milt_cv(milt_model("naive"), s, folds = 5L, horizon = 12L)
print(cv)
}
}
\seealso{
\code{\link[=milt_backtest]{milt_backtest()}}, \code{\link[=milt_compare]{milt_compare()}}

Other model: 
\code{\link{milt_backtest}()},
\code{\link{milt_compare}()},
\code{\link{milt_ensemble}()},
\code{\link{milt_fit}()},
\code{\link{milt_forecast}()},
\code{\link{milt_local_model}()},
\code{\link{milt_model}()},
\code{\link{milt_predict}()},
\code{\link{milt_refit}()},
\code{\link{milt_residuals}()}
}
\concept{model}
