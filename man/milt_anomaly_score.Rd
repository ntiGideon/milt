% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly-model.R
\name{milt_anomaly_score}
\alias{milt_anomaly_score}
\title{Compute a per-timestep anomaly score from residuals}
\usage{
milt_anomaly_score(residuals, method = c("norm", "difference", "nll_gaussian"))
}
\arguments{
\item{residuals}{Numeric vector of residuals (e.g. from \code{\link[=milt_residuals]{milt_residuals()}}).}

\item{method}{Character. One of:
\itemize{
\item \code{"norm"} (default): absolute residual, \code{abs(residuals)}.
\item \code{"difference"}: the signed residual itself (useful for
\code{\link[=milt_check_seasonality]{milt_check_seasonality()}}-style directional analysis).
\item \code{"nll_gaussian"}: negative log-likelihood of each residual under a
Gaussian fit to the whole residual distribution — penalises residuals
that are unlikely even relative to the model's own typical error.
}}
}
\value{
A numeric vector the same length as \code{residuals} (with \code{NA}
preserved where \code{residuals} is \code{NA}).
}
\description{
Turns a vector of forecast residuals (actual minus fitted/predicted) into a
continuous anomaly score, where a larger value means "more anomalous".
}
\examples{
s   <- milt_series(AirPassengers)
m   <- milt_model("naive") |> milt_fit(s)
milt_anomaly_score(milt_residuals(m), method = "norm")
}
\seealso{
\code{\link[=milt_anomaly_model]{milt_anomaly_model()}}

Other anomaly:
\code{\link[=milt_anomaly_model]{milt_anomaly_model()}},
\code{\link[=milt_causal_impact]{milt_causal_impact()}},
\code{\link[=milt_changepoints]{milt_changepoints()}},
\code{\link[=milt_detect]{milt_detect()}},
\code{\link[=milt_detect_anomalies]{milt_detect_anomalies()}},
\code{\link[=milt_detector]{milt_detector()}}
}
\concept{anomaly}
