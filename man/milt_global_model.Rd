% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-global.R
\name{milt_global_model}
\alias{milt_global_model}
\title{Fit one shared model across every group of a multi-series MiltSeries}
\usage{
milt_global_model(
  method = c("knn", "xgboost"),
  lags = 1:12,
  k = 5L,
  weights = "uniform",
  nrounds = 100L,
  max_depth = 6L,
  eta = 0.1,
  ...
)
}
\arguments{
\item{method}{Character. \code{"knn"} (pure R, no dependency) or \code{"xgboost"}.}

\item{lags}{Integer vector of lag indices used as features. Default \code{1:12}.}

\item{k, weights}{Hyperparameters for \code{method = "knn"}.}

\item{nrounds, max_depth, eta}{Hyperparameters for \code{method = "xgboost"}.}

\item{...}{Additional hyperparameters.}
}
\value{
An unfitted \code{MiltModel}. Fit it with \code{\link[=milt_fit]{milt_fit()}} on a grouped
\code{MiltSeries}; forecast a single group with \verb{$forecast()} or every group
at once with \verb{$forecast_all()}.
}
\description{
The "global" counterpart to \code{\link[=milt_local_model]{milt_local_model()}}: instead of training one
independent model per group, every group's lag-feature rows are pooled
into a single training matrix (tagged with a one-hot group indicator, plus
any static covariates attached via \code{\link[=milt_add_covariates]{milt_add_covariates()}} as
constant-per-group columns) and \strong{one} shared model is fit across all of
them — genuine cross-series weight sharing, the way darts' global
forecasting models work.
}
\examples{
\donttest{
tbl <- data.frame(
  date  = rep(seq(as.Date("2020-01-01"), by = "month", length.out = 36), 3),
  value = c(cumsum(rnorm(36, 1)), cumsum(rnorm(36, 2)), cumsum(rnorm(36, 0.5))),
  store = rep(c("A", "B", "C"), each = 36)
)
s <- milt_series(tbl, time_col = "date", value_cols = "value", group_col = "store")

gm <- milt_global_model("knn", lags = 1:6, k = 3L) |> milt_fit(s)
forecasts <- gm$forecast_all(horizon = 6)
forecasts[["A"]]
}
}
\seealso{
\code{\link[=milt_local_model]{milt_local_model()}}, \code{\link[=milt_add_covariates]{milt_add_covariates()}}

Other model:
\code{\link[=milt_backtest]{milt_backtest()}},
\code{\link[=milt_compare]{milt_compare()}},
\code{\link[=milt_conformal]{milt_conformal()}},
\code{\link[=milt_cv]{milt_cv()}},
\code{\link[=milt_ensemble]{milt_ensemble()}},
\code{\link[=milt_fit]{milt_fit()}},
\code{\link[=milt_forecast]{milt_forecast()}},
\code{\link[=milt_grid_search]{milt_grid_search()}},
\code{\link[=milt_local_model]{milt_local_model()}},
\code{\link[=milt_model]{milt_model()}},
\code{\link[=milt_predict]{milt_predict()}},
\code{\link[=milt_refit]{milt_refit()}},
\code{\link[=milt_residuals]{milt_residuals()}}
}
\concept{model}
