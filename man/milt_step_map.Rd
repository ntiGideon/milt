% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features-transform.R
\name{milt_step_map}
\alias{milt_step_map}
\title{Apply an arbitrary elementwise function to a time series}
\usage{
milt_step_map(series, fn, inverse_fn = NULL)
}
\arguments{
\item{series}{A \code{MiltSeries} object.}

\item{fn}{A function of one argument (\code{value}) or two arguments (\code{time},
\code{value}).}

\item{inverse_fn}{Optional function satisfying \code{inverse_fn(fn(x)) == x}
(or the two-argument equivalent), e.g. \code{expm1} to invert \code{log1p}. When
\code{NULL} (default), \verb{$step} is \code{NULL}.}
}
\value{
A named list:
\itemize{
\item \verb{$series} — the mapped \code{MiltSeries}.
\item \verb{$step}   — a \code{MiltMapStep} for inverting the transform, or \code{NULL}
when \code{inverse_fn} was not supplied.
}
}
\description{
Applies \code{fn} to every value in each value column. \code{fn} may take either one
argument (the values only, e.g. \code{log1p}) or two arguments (the timestamps
and the values, e.g. \code{function(time, value) value / lubridate::year(time)})
— the number of arguments \code{fn} declares is detected automatically, mirroring
darts' dual-mode \code{TimeSeries.map()}. Supply \code{inverse_fn} to get back an
invertible \code{MiltMapStep}.
}
\examples{
s   <- milt_series(AirPassengers)
out <- milt_step_map(s, fn = log1p, inverse_fn = expm1)
s_mapped   <- out$series
s_original <- out$step$inverse_transform(s_mapped)

# time-aware mapping: zero out the first year of observations
out2 <- milt_step_map(s, fn = function(time, value) {
  ifelse(lubridate::year(time) == lubridate::year(min(time)), 0, value)
})
}
\seealso{
\code{\link[=milt_step_boxcox]{milt_step_boxcox()}}, \code{\link[=milt_step_diff]{milt_step_diff()}}

Other features:
\code{\link[=milt_step_boxcox]{milt_step_boxcox()}},
\code{\link[=milt_step_calendar]{milt_step_calendar()}},
\code{\link[=milt_step_diff]{milt_step_diff()}},
\code{\link[=milt_step_fourier]{milt_step_fourier()}},
\code{\link[=milt_step_lag]{milt_step_lag()}},
\code{\link[=milt_step_rolling]{milt_step_rolling()}},
\code{\link[=milt_step_scale]{milt_step_scale()}},
\code{\link[=milt_step_unboxcox]{milt_step_unboxcox()}},
\code{\link[=milt_step_undiff]{milt_step_undiff()}},
\code{\link[=milt_step_unscale]{milt_step_unscale()}}
}
\concept{features}
