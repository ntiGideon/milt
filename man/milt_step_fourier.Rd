% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features-fourier.R
\name{milt_step_fourier}
\alias{milt_step_fourier}
\title{Add Fourier-term features to a MiltSeries}
\usage{
milt_step_fourier(series, period = NULL, K = 4L)
}
\arguments{
\item{series}{A \code{MiltSeries}.}

\item{period}{Positive number. The seasonal period in units of the series
frequency (e.g. \code{12} for annual seasonality in monthly data, \code{7} for
weekly seasonality in daily data).  Defaults to the series frequency
when \code{NULL}.}

\item{K}{Positive integer. Number of Fourier pairs (harmonics) to include.
Higher \code{K} captures more complex seasonal shapes. Must satisfy
\code{K <= floor(period / 2)}. Default \code{4L}.}
}
\value{
An augmented \code{MiltSeries} with columns \code{.fourier_sin_1},
\code{.fourier_cos_1}, ..., \code{.fourier_sin_K}, \code{.fourier_cos_K} appended.
Attribute \code{"milt_step_fourier"} stores the step specification.
}
\description{
Appends \code{2 * K} columns of sine and cosine terms at harmonics
\verb{k = 1, 2, ..., K} of the given seasonal \code{period}.  No rows are dropped
(all terms are defined for every time step).
}
\details{
Fourier features are a compact, continuous representation of seasonality and
are particularly useful for ML and DL models that cannot natively model
seasonal patterns.
}
\examples{
s     <- milt_series(AirPassengers)
s_fft <- milt_step_fourier(s, period = 12, K = 2)
head(s_fft$as_tibble())
}
\seealso{
\code{\link[=milt_step_lag]{milt_step_lag()}}, \code{\link[=milt_step_rolling]{milt_step_rolling()}}, \code{\link[=milt_step_calendar]{milt_step_calendar()}}

Other features: 
\code{\link{milt_step_calendar}()},
\code{\link{milt_step_lag}()},
\code{\link{milt_step_rolling}()},
\code{\link{milt_step_scale}()},
\code{\link{milt_step_unscale}()}
}
\concept{features}
