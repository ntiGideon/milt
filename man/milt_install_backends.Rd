% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-backends.R
\name{milt_install_backends}
\alias{milt_install_backends}
\title{Install optional backend packages for milt}
\usage{
milt_install_backends(backends = "all", upgrade = FALSE, quiet = FALSE)
}
\arguments{
\item{backends}{Character vector of group name(s) or package name(s) to
install.  Use \code{"all"} (default) to install every optional backend.}

\item{upgrade}{Logical.  If \code{TRUE}, upgrade packages that are already
installed to their latest CRAN version.  Default \code{FALSE}.}

\item{quiet}{Logical.  Suppress per-package install output.  Default \code{FALSE}.}
}
\value{
Invisibly returns a named logical vector indicating which packages
were newly installed (\code{TRUE}) vs already present (\code{FALSE}).
}
\description{
milt keeps heavy dependencies (Prophet, XGBoost, torch, …) in \code{Suggests} so
that installing milt itself is always fast and reliable.  Call
\code{milt_install_backends()} to install whichever backend groups you need.
\subsection{Backend groups}{\tabular{ll}{
   Group \tab Packages installed \cr
   \code{"forecasting"} \tab forecast, prophet \cr
   \code{"ml"} \tab xgboost, lightgbm, glmnet, ranger, e1071 \cr
   \code{"deep_learning"} \tab torch \cr
   \code{"extras"} \tab isotree, dbscan, dtw, CausalImpact, changepoint \cr
   \code{"reporting"} \tab rmarkdown, shiny, plumber, jsonlite \cr
   \code{"all"} \tab everything above \cr
}


You can also pass individual package names directly (e.g.
\code{milt_install_backends("prophet")}).
}

\subsection{torch / GPU note}{

After installing the \code{"deep_learning"} group (or \code{"torch"} directly), run
\code{torch::install_torch()} once to download the Lantern runtime.
}
}
\examples{
\dontrun{
# Install everything
milt_install_backends()

# Just the classical forecasting backends
milt_install_backends("forecasting")

# Just prophet
milt_install_backends("prophet")

# ML + deep learning together
milt_install_backends(c("ml", "deep_learning"))
}

}
\seealso{
\code{\link[=milt_model]{milt_model()}}, \code{\link[=milt_fit]{milt_fit()}}, \code{\link[=milt_forecast]{milt_forecast()}}
}
