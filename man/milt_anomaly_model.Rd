% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly-model.R
\name{milt_anomaly_model}
\alias{milt_anomaly_model}
\title{Wrap a model with residual-based anomaly detection}
\usage{
milt_anomaly_model(
  model,
  scorer = c("norm", "difference", "nll_gaussian"),
  detector = c("quantile", "threshold"),
  quantile = 0.95,
  low = NULL,
  high = NULL,
  ...
)
}
\arguments{
\item{model}{An unfitted \code{MiltModel} (created with \code{\link[=milt_model]{milt_model()}}). Cloned
internally; the object passed in is not modified.}

\item{scorer}{Character. Passed to \code{\link[=milt_anomaly_score]{milt_anomaly_score()}}: \code{"norm"}
(default), \code{"difference"}, or \code{"nll_gaussian"}.}

\item{detector}{Character. \code{"quantile"} (default): threshold at the
\code{quantile} quantile of in-sample scores. \code{"threshold"}: use explicit
\code{low}/\code{high} bounds.}

\item{quantile}{Numeric in \verb{(0, 1)}. Used when \code{detector = "quantile"}.
Default \code{0.95}.}

\item{low, high}{Numeric bounds used when \code{detector = "threshold"}.}

\item{...}{Additional arguments forwarded to \code{model$fit()}.}
}
\value{
An unfitted \code{MiltModel}. Fit it with \code{\link[=milt_fit]{milt_fit()}}, then call
\code{\link[=milt_detect_anomalies]{milt_detect_anomalies()}} to get a \code{MiltAnomalies} result.
}
\description{
Fits \code{model} on a series, scores its in-sample residuals with
\code{\link[=milt_anomaly_score]{milt_anomaly_score()}}, and fits a threshold from that score distribution —
"flag the points where my own model's prediction was most wrong." Mirrors
darts' \code{ForecastingAnomalyModel}.
}
\examples{
\donttest{
s  <- milt_series(AirPassengers)
am <- milt_anomaly_model(milt_model("naive"), scorer = "norm") |> milt_fit(s)
anoms <- milt_detect_anomalies(am)
plot(anoms)
}
}
\seealso{
\code{\link[=milt_detect_anomalies]{milt_detect_anomalies()}}, \code{\link[=milt_anomaly_score]{milt_anomaly_score()}},
\code{\link[=milt_detector]{milt_detector()}}

Other anomaly:
\code{\link[=milt_anomaly_score]{milt_anomaly_score()}},
\code{\link[=milt_causal_impact]{milt_causal_impact()}},
\code{\link[=milt_changepoints]{milt_changepoints()}},
\code{\link[=milt_detect]{milt_detect()}},
\code{\link[=milt_detect_anomalies]{milt_detect_anomalies()}},
\code{\link[=milt_detector]{milt_detector()}}
}
\concept{anomaly}
