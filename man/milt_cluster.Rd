% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{milt_cluster}
\alias{milt_cluster}
\title{Cluster multiple time series}
\usage{
milt_cluster(series_list, k, method = "euclidean", max_iter = 100L, ...)
}
\arguments{
\item{series_list}{A list of \code{MiltSeries} objects (each univariate).}

\item{k}{Integer. Number of clusters.}

\item{method}{Character. Clustering method:
\itemize{
\item \code{"dtw_kmeans"} — k-means with DTW distance (requires \code{dtw} package).
\item \code{"kshape"} — shape-based clustering (no extra package).
\item \code{"feature_based"} — extract time series features then k-means.
\item \code{"euclidean"} — k-means on raw aligned series (all must be equal length).
}}

\item{max_iter}{Integer. Maximum k-means iterations. Default \code{100L}.}

\item{...}{Additional arguments (unused).}
}
\value{
A \code{MiltClusters} object.
}
\description{
Partitions a list of \code{MiltSeries} objects into \code{k} groups using one of
four algorithms.
}
\examples{
\donttest{
# Create 4 slightly different series
make_s <- function(offset) {
  milt_series(AirPassengers + offset)
}
series_list <- lapply(c(0, 10, 20, 30), make_s)
cl <- milt_cluster(series_list, k = 2, method = "euclidean")
print(cl)
}
}
\seealso{
\code{\link[=milt_classifier]{milt_classifier()}}
}
\concept{cluster}
