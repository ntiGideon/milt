% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-impact.R
\name{milt_causal_impact}
\alias{milt_causal_impact}
\title{Estimate the causal impact of an intervention}
\usage{
milt_causal_impact(series, event_time, covariates = NULL, n_seasons = 0L, ...)
}
\arguments{
\item{series}{A \link{MiltSeries} object (univariate).}

\item{event_time}{The time at which the intervention occurred.  Must be a
value present in \code{series$times()}.  The pre-period is everything strictly
before \code{event_time}; the post-period is \code{event_time} onwards.}

\item{covariates}{Optional \code{MiltSeries} object (or numeric matrix with the
same number of rows as \code{series}) providing control covariates.  Passed
to \code{CausalImpact} as additional columns in the data matrix.}

\item{n_seasons}{Integer.  Number of seasons for the seasonal component.
\code{0L} (default) disables seasonality.}

\item{...}{Additional arguments forwarded to \code{\link[CausalImpact:CausalImpact]{CausalImpact::CausalImpact()}}.}
}
\value{
A \code{MiltCausalImpact} object.
}
\description{
Fits a Bayesian structural time-series model on the pre-intervention period
of \code{series} and extrapolates it as the counterfactual for the
post-intervention period.  The difference between actual and counterfactual
estimates the causal effect.
}
\details{
Requires the \code{CausalImpact} package (>= 1.2.7).
}
\examples{
\donttest{
s <- milt_series(AirPassengers)
ci <- milt_causal_impact(s, event_time = as.Date("1956-01-01"))
plot(ci)
}
}
\seealso{
\code{\link[=milt_changepoints]{milt_changepoints()}}

Other anomaly: 
\code{\link{milt_changepoints}()},
\code{\link{milt_detect}()},
\code{\link{milt_detector}()}
}
\concept{anomaly}
